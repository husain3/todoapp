extends layout

block content
    h1= title
    form#formAddTodo(name="addtodo",method="post",action="/addtodo")
        input#inputNewTodo(type="text", placeholder="New Todo", name="New Todo")
        button#btnSubmit(type="submit") submit
